{
  "name": "kcal-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "Log food by typing it, scanning a barcode, or snapping a photo, and track daily calories/macros against a goal.",
  "license": "MIT",
  "scripts": {
    "dev": "node --env-file=.env dev-server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
